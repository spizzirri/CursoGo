package service_test

import (
	"testing"
)

func TestAddUser(t *testing.T){
/*
	assert := assert.New(t)
	var userManager service.UserManager

	validation := userManager.addUser("damian", "123aaa")
	if validation != false{
		t.Error("The user can not be add")
	}

	user := userManager.getUsers("damian")

	assert.True(user)
*/
}

